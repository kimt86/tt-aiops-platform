# tt-aiops-platform 작업 규약

말레이시아 컨테이너 터미널의 야드 트랙터(TT) 배차·분석 플랫폼.

**목표**: 우리가 계산한 배차 결과를 TOS가 확인하고, **그 결과로 실제 배차를 할 수 있게** 하는 것.

**현재 상태**: 아직 그 배선 전이라 TOS에 쓰기가 없고 현장 배차를 바꾸지 않는다(그림자).
다만 **"지금은 그림자니까"를 판단 근거로 쓰지 않는다.** 우리 출력은 배차 지시가 될 것이므로:

- **시점이 곧 지시다.** 필요보다 일찍 내면 트럭이 크레인 앞에서 기다린다.
  "언제 보낼지"는 부수적 정보가 아니라 출력의 본체다.
- **TOS를 경쟁자나 제약으로 모델링하지 않는다.** TOS가 무엇을 배차했는지와 무관하게,
  현재 상황에서 배차해야 할 작업을 우리가 판단한다. 작업 목록도 TOS의 배차 상태에
  묶이면 안 된다(2026-08-03: 목록이 'TOS 미배차'로 한정돼 있어, TOS가 25분 전에
  가져간 작업이 우리 눈에 들어오기 전에 사라졌다).
- **TOS를 잘 흉내내는 것이 목표가 아니다.** 합격 기준을 "TOS와 얼마나 같은가"로 잡으면
  TOS를 넘어설 수 없다.
- 그림자라서 안전한 것은 **배포 사고**뿐이다. 잘못된 판단은 그대로 남아 나중에 실린다.

---

## 수치 계산

확률·스코어·통계값은 **추론으로 계산하지 않는다.** 반드시 코드를 실행하고 그 출력값을 근거로 제시한다.

중간 계산이 여러 단계를 거칠 경우, **요약문의 숫자를 재인용하지 말고 원본 출력에서 다시 읽어온다.**

### 비율을 보고하기 전 — 분모를 한 문장으로 쓴다

이 프로젝트에서 실제로 반복된 실패는 산술이 아니라 **분모·비교 대상 선정**이었다. 코드를 돌려도 질의가 틀리면 결과는 틀린다.

보고 전에 이 셋을 확인한다:

1. **분모가 무엇인가** — "X의 몇 %"에서 X를 한 문장으로 쓸 수 있어야 한다.
2. **두 자료의 시점이 같은가** — 현재 스냅샷 표와 과거 이력 표를 맞대면 착시가 난다.
   같은 질의를 **시간 창을 좁혀 한 번 더** 돌려 값이 안정적인지 본다.
3. **층화 변수가 결과에서 파생됐는가** — 잔차를 잔차의 함수로 가르면 동어반복이다.
   확인법: 층화 변수와 결과 부호의 일치율을 재고, 그 변수를 안 쓰는 위약(placebo)으로도
   같은 결과가 나오는지 본다.

실제 사례(2026-08-03~04, 전부 코드를 돌려서 얻은 틀린 숫자):
- "보정의 90%가 트럭 지연" → 층화 기준이 정답 컬럼 그 자체 (잔차를 부호로 가름)
- "크레인 작업의 40%가 목록에 없다" → 완료된 구역이 현재 스냅샷에서 빠진 것
- "98% 순서대로 한다" → 이동하지 않은 경우를 분모에 포함
- "34.5%가 뒤로 간다" → 같은 베이 안 이동을 되돌아감으로 계수

---

## 단정 금지

코드베이스에서 **실제로 확인하지 않은 사항은 단정하지 않는다.**
**"확인했다"와 "~일 것이다"를 구분해서 표현한다.**

- 근거가 하나면 "~로 보인다 / 가설이다". 독립된 근거가 둘 이상일 때만 단정한다.
- **측정 사실과 원인 해석은 문장을 나눈다.** "X초로 측정됐다"까지가 사실이고
  "그래서 Y가 원인이다"는 별도 문장이다.
- "중대 사항"이라는 표현은 **반증을 한 번 시도해 통과한 뒤에만** 쓴다.
  사용자가 그 말에 반응해 실제로 행동한다(2026-08-03: 이 표현 때문에 정상 동작하던
  실험이 중지됐고, 이후 진단이 틀린 것으로 드러났다).
- 이미 낸 단정이 틀리면 즉시 철회하고 무엇이 남고 무엇이 사라졌는지 구분한다. 자책은 짧게.

---

## 범위 준수

**요청받은 범위 밖은 건드리지 않는다.**

기존 설계가 잘못되어 보여도 **임의로 변경하지 말고, 먼저 지적하고 승인을 받는다.**

- 지시가 "조사"면 조사만 한다. 조사 중 고칠 곳이 보여도 보고까지가 끝이다.
- 지시가 "A를 재라"면 A를 잰다. B가 더 흥미로워 보여도 A를 먼저 끝낸다.

---

## 이 저장소의 사실

### 빌드·테스트

```
cargo build --release -p tt-api        # API 서버 (바이너리 이름은 api)
cargo build --release -p tt-extractor  # TOS 추출기 (바이너리 이름은 extractor)
cargo test --workspace                 # 47개, 전부 통과가 기준
```

⚠ 패키지 이름과 바이너리 이름이 다르다. `-p tt-scengen`처럼 **없는 패키지를 지정하면
아무것도 빌드되지 않는데 유닛은 옛 바이너리로 "success"를 반환한다.**
배포 확인은 `target/release/<bin>`의 mtime으로 한다.

프론트: `cd web && npm run build`

### 배포

systemd **사용자** 유닛(`tt-*`). 유닛 파일은 심링크가 아니라 **복사본**이다:

```
cp deploy/systemd/<unit> ~/.config/systemd/user/ && systemctl --user daemon-reload
systemctl --user restart tt-api
```

### 데이터베이스

`wp_tt` @ 127.0.0.1:5433 (user `wp`). **조사용 사본이 아니라 운영 DB다.**

- **인덱스 먼저 → 측정 → 스키마 변경** 순서를 지킨다. 뒤집으면 조회가 스키마 변경을
  막고, 그게 API의 라이브 쓰기를 막는다(2026-08-03 실제 사고: 4분 정지).
- 새 조인 키를 쓰기 전 `pg_indexes`로 인덱스를 확인한다. **PK가 있어도 선두 컬럼이
  아니면 못 쓴다.**
- 무거운 탐색 질의는 `BEGIN; SET LOCAL statement_timeout=...; EXPLAIN (ANALYZE) ...; ROLLBACK;`
  으로 계획을 먼저 본다.
- `ALTER TABLE` 전에 `pg_stat_activity`로 그 표에 도는 세션이 없는지 확인한다.

마이그레이션은 전용 러너가 없다. `psql -f db/migrations/<파일>`로 직접 적용하며,
**전부 멱등이어야 한다**(두 번 돌려도 안전).

### TOS(Oracle)

**읽기 전용.** 저부하 직접 조회는 허용되나 항상 범위를 제한한다. `crates/api`는
설계상 Oracle 접근 수단이 없다(전 접근은 `crates/extractor` 경유).

### git

여러 에이전트가 같은 워크트리를 쓸 수 있다. **`git commit -- <경로>`로 경로를 지정한다** —
인덱스가 공유 자원이라 남의 스테이징이 딸려 들어간다(커밋 `acb8677` 실제 사고).

### 시나리오/`crates/scengen`

**2026-08-12 이관 — 이 에이전트 담당이다**(종전 "다른 에이전트 담당·손대지 않는다"는 폐기).
작업 워크트리는 `~/projects/tt-scengen`(브랜치 `scengen`), **그 워크트리의 빌드는 배포되지 않는다.**

---

## 도메인 용어

| 용어 | 뜻 |
|---|---|
| **DS** | 양하(discharge) — 배 → 야드 |
| **LD** | 적하(load) — 야드 → 배 |
| QC | 안벽 크레인 · RTG | 야드 크레인 · TT | 야드 트랙터 |
| 2단계 배차 | 1단계=수요·긴급도 선정 / 2단계=비용행렬 매칭 |

### ★트럭 작업 4단계 — 이 용어만 쓴다 (2026-08-04 지정)

```
무부하주행 ──▶ 픽업 ──▶ 부하주행 ──▶ 드랍오프
 (빈 차로 감)  (실음)    (싣고 감)     (내림)
```

작업 유형별로 각 단계가 어디서 일어나는지:

| 단계 | 적하(LD) | 양하(DS) |
|---|---|---|
| 무부하주행 | → 야드 블록 | → 안벽 QC |
| **픽업** | RTG가 실어줌 | **QC가 실어줌** |
| 부하주행 | → 안벽 | → 야드 블록 |
| **드랍오프** | **QC가 내림** | RTG가 내림 |

**⇒ QC가 관여하는 단계가 반대다.** 적하는 QC가 **드랍오프**를, 양하는 QC가 **픽업**을 한다.
배차 마감(= QC가 이 상자를 다루는 시각)이 적하는 사슬 끝, 양하는 사슬 중간에 있다는 뜻이고,
그래서 필요한 준비시간이 크게 다르다(실측 적하 ~1,448초 / 양하 ~455초).

**트럭 준비시간** = 할당부터 QC가 다룰 때까지 = 적하는 `무부하주행 + 픽업 + 부하주행`,
양하는 `무부하주행`(+ 픽업 대기). 배차 마감은 QC 시각에서 이걸 뺀 값이다.

⚠ 코드에 남아 있는 나쁜 이름: `lead_extra_s` / `extra_s` — "여분"이라는 뜻인데 무엇에 대한
여분인지가 없다. 실제로는 **무부하주행 이후 QC 인계까지**의 시간이다. 새로 쓰는 곳에서는
이 용어표대로 이름 짓고, 기존 것은 정리할 때 함께 고친다.

**설계 규율: 긴급도·기아·부하분산은 2단계 간선 비용에 넣지 않는다.** 긴급도는 1단계
선정에서만 결정한다.

시각은 터미널 현지(MYT, UTC+8) 기준으로 표시한다.

### 정답지(ground truth) — 틀리기 쉬운 것

- **작업 도달 시각**: `qc_move_log.comp_ts`. ⚠ 같은 표의 배정시각 컬럼은 2026-08-10에
  `dispatch_ts`로 개명됐다(구명 `st_ts` — "시작"으로 읽혀 사고가 반복돼 값에 이름을 맞춤,
  mig0147). **크레인 물리 시작은 TOS 어디에도 없다**(발굴조사 확정) — 필요하면 추정
  (`sql/local/l_qc_q.sql`). RTG(`rtg_move_log`)의 `st_ts`는 반대로 진짜 물리 시작이니 혼동 금지.
- **트럭이 자유로워진 시각**: `tt_move_log.free_ts`(TOS). GPS `tt_cycle_log.dropped_at`은
  33%를 놓치므로 채점에 쓰지 않는다.
- 새 정답지를 채택하기 전 **KC 문서(`kc/data/tos-db-reference.html` 등)를 먼저 읽는다.**
  컬럼 이름으로 의미를 추정하다 한 번 크게 틀렸다.

---

## 문서

`kc/`(지식센터)는 중고등학생 수준의 쉬운 말로 쓴다. 내부 변수명이나 임의로 만든 용어를
그대로 노출하지 않는다. 추출·배차 로직을 바꾸면 해당 KC 문서도 같이 갱신한다.
