# 발행 2계층 — 남는 트럭에 마감 미도래 발행 지시를 미리 배정 (mig 0161)

2026-08-25. 사용자 확정 설계: 1계층 = 마감 도래 슬롯(현행·항상 우선) → 남는 트럭을
2계층 = 나머지 전 발행 지시(마감 이른 순·트럭 수까지)에. 각 층 비용 = 순수 이동시간
(튜닝 상수 없음). 목표 = "TOS 배차 순간 우리 유효 추천 없음"(none·reco_src) **DS 55.6 /
LD 65.9%**(08-25 진단) 해소 — 적하 none 의 81%가 "직전 30분 그 큐에 발행 자체가 없던"
경우였고, 2계층이 정확히 그 구역을 덮는다. pull 2/2 본체.

## 배선 (커밋 09b297f · 7bcdd1c)

- 배분: `allocate_two_tier()`(livemap.rs·단위테스트 4건) — 1계층을 다 채운 뒤에만 잔여
  트럭이 2계층으로. 합계 ≤ truck_n(재지향 제외) — "작업>트럭 금지"는 층을 합쳐 성립.
- 솔브는 **순차 2회**: 1계층을 전 트럭에서 먼저 최적으로 풀고(입력이 종전과 동일 = 동작
  보존), 그 잔여 트럭만으로 2계층을 푼다. 한 행렬에 섞으면 2계층이 1계층 트럭을 뺏는다.
- **재지향(pool_ver 8) 트럭은 2계층 제외** — 긴급하지 않은 일로 TOS 배차를 트는 것은
  손해뿐(간선 자체를 안 만든다·그리디도 동일 규칙).
- 판별자: `stage2_match_shadow.match_tier`(1/2·NULL=경계 이전) ·
  `stage2_solver_shadow.t2_works/t2_slots/t2_assign_n`(NULL=경계 이전) ·
  `dispatch_compare_shadow.reco_tier`(none 하락의 2계층 귀속용).
- 소비처 거름(판별자 규율): 지도 추천선(advisory)·30분 요약·헬스 지표·채택률 분모는
  **1계층만**(2계층은 feasible ~100%·마감 수 시간 뒤의 예고라 종전 잣대를 오염시킨다).
  순간 비교(reco_*)만 의도대로 2계층 포함. 보드는 상위 40(급한 순)이라 자연히 1계층 위주,
  2계층이 뜨면 "미리" 칩. stage2_pool_shadow 는 1계층만(표 정의 보존).
- KC 4쪽 갱신(dispatch-deadline ④단계·stage2-journey/design·board 칩).

## 경계

- **2026-08-25 15:20:02 MYT(07:20:02Z) 첫 2계층 틱**(배포 15:19:54 MYT). 소비처 게이트
  재배포 15:23:06 MYT. 판별은 시각이 아니라 컬럼 NULL 여부로 한다.

## 첫 측정 (배포 직후 ~4틱)

- 배분(로그·솔버 게이지): truck_n 244~297 · 1계층 81~85 슬롯(종전과 동일) · **2계층
  163~170 슬롯 전량 매칭** · trucks_held **167~181 → 0**. optimal_n 85 → 245~248.
- 틱 페이스 60초 유지(행렬 ~3배에도 지연 없음·재시작 직후 포함). INSERT 오류 0.
- ⑮ 풀 재현율(트럭 축·이 변경과 무관해야 함): **pool_ver 8 창 95.7%**(n=1,756·13:45~15:23
  MYT) — 회귀 없음. 놓침의 98.7% = 자유 뒤 90초 안 등재(알려진 예측 꼬리).
- 지도 추천선 77개(=1계층만)·보드 상위 40 전부 1계층 — 게이트 동작 확인.

## 남은 측정 (이 사이클 안·야간 포함 긴 창)

- **none 재측정**: 경계 후 ≥24h 지향, `reco_src` 로 가르고 `reco_tier` 로 2계층 몫 귀속.
  경계 직후 15분의 none 은 대부분 경계 이전 추천 스트림을 되감은 것이라 섞지 말 것
  (reco 조회 창 15분 + 유효 150초).
- ⑮ 재현율 야간 포함 재확인(회귀 감시) · 채택률 시계열이 게이트 후 연속적인지.
- 관찰: 2계층 추천의 "예고 → 실제 그 트럭이 그 일을 받았나"(reco_tier=2 & agree) —
  발행 페이스 vs 크레인 리듬 괴리가 얼마나 메워지는지의 직접 계기.

## 1차 리뷰 반영 (FIX FIRST 3 + SHOULD_FIX 4 + CONSIDER 3 — 전부 수용)

- **(차단1) prev 오염 수정**: 2계층 행이 들락날락 방지(prev)에 섞여 1계층 switched 가
  0.05%→4.5%로 뛰었다(리뷰 실측·같은 90분 창). 수정 = 1계층 간선은 **2계층 행을 뺀 prev**
  (경계 이전 NULL 행 포함 → 종전 입력과 동일 = 동작 보존 복원), 2계층 간선은 계층 불문
  최신(prev_any·예고 자체의 안정성). ⚠**오염 창 = 15:20:02~15:41:32 MYT** —
  이 창의 1계층 switched 는 오염값이니 시계열 비교에서 제외할 것.
- **(차단2·3) 게이트 누락 2곳**: 보드 라이브 채택률 분모·VS TOS 상자 비교(BOX_JOIN_CTE)
  first_* — 1계층만으로 게이트(박제 쿼리와 같은 잣대 복원). truck_match/ytno_match 의
  EXISTS 는 계층 불문 유지(그 트럭 추천이 서 있었다는 사실 자체는 계층과 무관).
- 헬스 화면 내부 일관화(히스토그램·트렌드·decisions·matches_latest 게이트) ·
  stage2_shadow 상세 목록에 match_tier 표식 · 러닝센터 s2_feasible_pct 게이트 ·
  분석 스크립트 2개(counterfactual_wait·deadline_axis_compare) 계층 필터 ·
  self_cover_n 정의 보존 · rank_new 1계층만(양층 걸친 묶음의 덮어씀) ·
  mig0161 COMMENT 정정(greedy_n 정확 복원 불가 명시) · 솔버 실패 힌트에 0161.

## RESULT (2026-08-26 07:23 MYT 확정 · 야간 포함 ~16h 창)

**none(TOS 배차 순간 유효 추천 없음)이 절반 이하로 떨어졌다 — 목표 달성.**

- **분모**: 비교기가 채점한 TOS 배차 사건((qc,queuename,tos_ytno,t1_ts) 단위 최초 채점 행·
  t1_ver=1·평가 가능(reco_src NOT NULL)·DS/LD). 같은 질의를 경계 전/후에 적용.
- **none**: DS **59.0 → 29.5%**(n=2,779→4,607) · LD **67.9 → 32.4%**(n=4,236→11,911).
  전 창 = 08-25 10:00~15:20 MYT(reco_* 시작~경계) / 후 창 = 08-25 15:45~08-26 07:23 MYT.
  (원진단 55.6/65.9 와 전 창 수치가 3%p 안팎 다른 것은 중복 제거 방식 차이 — 비교는 동일 질의로.)
- **귀속**: 후 창 전체 사건의 DS 55.6%/LD 45.6%가 **reco_tier=2**(2계층 예고가 서 있었음) —
  하락분이 2계층에서 왔음이 표식으로 직접 확인된다. 원진단 메커니즘("none 81%=미발행 큐")과 부합.
- **분모 건전성**: 평가불능 비율 전 1.0%/후 1.1% — 분모 이동으로 만든 착시 아님.
- **야간 안정**: 3h 구간별 none 20.8~39.5%(00~06시 MYT 가 오히려 최저) — 야간 회귀 없음.
- **⑮ 풀 재현율(야간 포함)**: **96.4%**(n=15,150·pool_ver 8 창 전체 08-25 13:45~08-26 07:23) —
  ver 7 의 96.4% 와 동일, 회귀 없음. 놓침 93.1% = 알려진 예측 꼬리(자유 뒤 90초 안 등재).
- **건강**: 1계층 switched 0.16%(기준선 0.23%·오염 창 5.16% 소멸) · 2계층 105~249건/틱 밤새
  지속·슬롯 못 받은 트럭 0 · 배포 후 신규 경보 0(13:53 MYT 추출기 단발 경고는 배포 전).
- **참고(과제 아님)**: 커버 사건의 트럭 일치는 0.6~2.8%로 낮다 — 양하는 실단위가 (QC큐,트럭수)
  라 상자×트럭 일치가 원래 맞는 잣대가 아니고, 적하 쪽은 추후 관찰.

**남은 none ~30%의 정체는 미진단** — 다음 사이클 후보(가설: 작업목록 밖 유형·no_coord 탈락·
발행 지시 자체가 없는 큐. 근거 미확보).
