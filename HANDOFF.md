# HANDOFF — 이번 사이클

마지막 갱신 2026-08-18. 앞선 사이클: 시스템 상태 평가 → **자문(Phase 1)은 가능, 실배차(Phase 2)는
불가** 판정. 막는 것은 안정성이 아니라 ①TOS 채널·계약 없음 ②출력의 본체인 **"시점"이 현장 눈금으로
검증되지 않음** ③"낫다"의 합의된 증거 없음. 이번 사이클은 ②를 우리 손으로 재는 것이다.

> 상시 사실·기준선·함정은 `~/.claude/notes/tt-aiops-platform.md` 에 있다(150줄·상한 도달).
> 이 파일은 **이번 사이클의 정의**만 담는다.

---

## GOAL

"우리 추천대로 그 시각에 그 트럭을 보냈다면 크레인 앞에서 몇 분 기다렸을까(또는 몇 분 늦었을까)"를
최근 7일 **상자 단위**로 재서, TOS 실제 배차와 **같은 잣대**의 숫자로 나란히 놓는다.

## IN SCOPE

1. 반복 실행 가능한 측정 질의 1개 — `sql/local/` 아래 새 파일. 입력은 이미 있는 표만:
   - 우리 추천: `stage2_match_shadow`(ts·ytno·contno·jobtype·arrival_s·lead_extra_s).
     **상자당 첫 추천**이 헤드라인("보냈다면 즉시 출발"), TOS 배차 직전 마지막 추천은 민감도.
   - 정답지: `tt_move_log`(dispatch_ts) ⋈ `qc_move_log.comp_ts` — 크레인이 그 상자를 다룬 시각.
   - 우리 트럭의 QC 도착 추정 = 추천시각 + `arrival_s` (+ 적하는 `lead_extra_s`).
2. 낼 숫자(분모를 문장으로 적어 보고):
   - **상한** = `comp_ts − 우리 도착` (크레인 순서 고정 가정 → 우리 트럭 대기, 음수면 늦음)
   - **하한** = `그 크레인의 직전 무브 comp_ts − 우리 도착` (앞 상자가 끝나기 전 도착이면 확실히 기다림)
   - **같은 상자의 TOS**: `comp_ts − dispatch_ts`(배차→처리·기존 실측 중앙 양하 6.7 / 적하 31.7분)와
     `우리 추천→처리`를 나란히 — 차이가 곧 우리가 더한 대기.
   - 양하/적하 분리 · 크레인 굶김 유무(직전 무브 간격 5분↑)로 층화.
3. **위약**: 같은 잣대를 TOS 배차에 적용(`dispatch_compare_shadow.tos_arrival_s` 로 TOS 트럭 도착
   추정) → 대기 중앙값이 ~0 부근이어야 한다. 안 오면 **잣대가 틀린 것**이지 우리가 이른 게 아니다.
4. 결과 기록: 이 파일 + 노트 "측정 기준선" 절. **KC 게시는 안 한다**(사용자 결정 — 판정 문턱을 정한
   뒤 다음 사이클에).

## OUT OF SCOPE

- 배차 로직·마감·풀 변경 일체. 라이브 게이지·매뷰·타이머 신설 없음.
- 예측 정확도(크레인 처리 시각 ±10~20분) 개선 없음.
- TOS 세션 자료(`docs/tos-integration-handoff.md`) 갱신 없음. KC 문서 변경 없음.

## DONE CRITERIA

```bash
psql -h 127.0.0.1 -p 5433 -U wp -d wp_tt -f sql/local/<파일>.sql     # 60초 안(SET LOCAL statement_timeout)
```
- 출력 한 표에 양하·적하 각각: **분모 상자 수 · 대기 상한/하한 중앙·p90 · TOS 배차→처리 중앙 ·
  우리 추천→처리 중앙**.
- 위약(TOS 자기 배차에 같은 잣대) 대기 중앙값이 같은 표에 있다.
- 층화 변수(굶김 유무)가 결과(대기)에서 파생되지 않았음을 한 줄로 밝힌다(직전 무브 간격은 우리 도착과
  무관 — 일치율로 확인).
- 판정 문장 하나: "우리 추천대로면 트럭 대기 중앙 X분 (TOS 대비 +Y분)". **수치를 낼 뿐, 문턱 통과
  여부는 이번에 정하지 않는다.**
- 상시 점검(변경 없어도 돌린다): `cargo test --workspace` 70 통과 · `systemctl --user is-active tt-api`
  active · 유닛 드리프트 0.

## RESULT — ★2026-08-19 재정의 (앞선 판정을 대체한다)

**사용자 지정**: 현장은 **차량이 작업을 고르는 pull 구조**다. 트럭이 비면 TOS 에 배차를 요청하고 TOS 가
그 순간 그 트럭에 작업을 준다. ⇒ **TOS 배차시각과의 비교는 의미가 없다**(그 시각은 결정 시점이 아니라
트럭이 비는 시점일 뿐). 합격 기준은 하나 — **트럭이 물어본 그 순간 우리에게 그 트럭에 줄 답이 있었나.**

pull 구조 실측 확인(3일·왕복): 자유 → 다음 배차 중앙 **15초(양하) / 38초(적하)**, 52~58%가 60초 안,
자유 전 배차 0.3% / 3.3%. ⇒ 배차 시점을 정하는 건 작업이 아니라 트럭이다.

### ① 헤드라인 — 차량 요청 시점 커버리지 (`scripts/pull_model_coverage.sql` · 7초)

분모 = 최근 7일 트럭이 요청해 작업을 받은 **왕복** 건(twin 첫 레그만): 양하 56,016 / 적하 50,908.

| | 양하 | 적하 |
|---|---|---|
| **커버리지 — 그 순간 우리에게 그 트럭 답이 있었음** | **19.9%** | **17.8%** |
| 같은 상자였음(부수·pull 에선 합격 기준 아님) | 0.1% | 0.2% |
| 직전 우리 틱 나이(중앙/p99) | 30 / 60초 | 30 / 60초 |

### ② 원인 분해 — 상한도 사각지대도 아니다

- **용량 상한 아님**: 우리는 틱마다 **34.6대**를 지목하는데 요청은 **11.3대/분**뿐이다(3배 여유).
- **함대 사각지대 아님**: 2일간 요청한 488대 중 **481대**가 우리 출력에 나온 적이 있다(전혀 없는 건 7대).
- **틱 주기(구조적)**: 놓친 건의 **39~46%** 는 트럭이 우리 직전 틱 **이후에** 비었다 — 60초 주기로는
  낼 수 없는 건이다(빈 뒤 15~38초 만에 요청하므로).
- **나머지 54~61% 는 틱 전에 이미 비어 있었다.** 그 건들은 빈 채로 중앙 **9~10분**을 서 있었는데도
  그 사이 우리가 그 트럭을 지목한 적은 **20%** 뿐이다. ⇒ 구조가 아니라 **선정이 다른 트럭을 보고 있다**.
- **GPS 침묵이 기전의 일부**(트럭은 정지하면 단말이 침묵한다 — 기존 확인 사항):

  | 요청 직전 GPS 나이 | 60초 이내 | 1~5분 | 5~30분 | 30분+ | 없음 |
  |---|---|---|---|---|---|
  | 커버리지 | 25.3% | 30.2% | 12.2% | **0.0%** | **0.0%** |
  | 건수(2일) | 15,473 | 5,641 | 7,758 | 2,437 | 310 |

  30분+ 침묵 트럭은 **한 건도 못 준다**(요청의 8.8%). 다만 GPS 가 신선해도 25~30% 라 **침묵만으로는
  설명되지 않는다**(측정 사실과 원인 해석을 나눠 적는다 — 아래는 가설).
- **가설**: 우리 출력의 **12.8% 만 지금 비어 있는(idle) 트럭**에 나간다(soon_* 68.6% · wait_rtg 18.6%).
  pull 구조에서 아직 안 빈 트럭을 미리 잡는 것은 그 순간에는 답이 아니다. 근거가 하나라 단정하지 않는다.

### ③ `comp_ts` 확정 + 픽업 동작 시간 (사용자 지적 2번)

- `comp_ts` = **트럭↔크레인 인계가 끝난 물리 시각**. 근거 2갈래: 추출기 헤더("comp_ts is the physical
  handover completion", `crates/extractor/src/qc_moves.rs:4`)와 KC 사이클 문서("픽업 완료를 크레인
  comp_ts 로 보정"). ⇒ 지적대로 **픽업 동작 시간이 '대기'에 들어가 있었다**.
- 순수 동작 시간 실측(트럭 GPS 도착 → comp_ts 의 하위 분위수·7일·n=17,635/18,903):
  **p05 양하 29초 / 적하 22초 · p10 48 / 40초.** 1~2분보다 짧다.
- 빼도 결론은 안 바뀐다: 반사실 대기 상한 중앙 **15.1 → 14.3분(양하) · 23.9 → 23.2분(적하)**.
- **관측 기준선(현행 TOS 운영)**: 트럭이 크레인에 도착해 인계가 끝날 때까지 중앙 **양하 5.1분 /
  적하 3.7분**, p90 ~30분. ⚠우리 값(①)은 모델 추정 도착이라 구성이 달라 같은 값처럼 읽으면 안 된다.

### ④ ★놓친 차량은 풀에 있었나, 없었나 (2026-08-19 사용자 질문 · 최근 2일·왕복 1건)

풀 정의(`livemap.rs:4729~`): GPS 신선(≤120초) + `classify_tt` 상태가 `idle`/`soon_idle`/`wait_rtg` 면 후보.
`delivering`/`empty_travel`/`staging` 는 코드에서 `continue` = 제외. GPS 가 낡으면 분류 자체가 안 된다.
`truck_pos_hist.state` 는 **매처와 같은 `classify_tt` 출력**이고 GPS 신선할 때만 기록되므로 풀 자격을 재현한다.

| 놓친 이유 | 양하 | 적하 |
|---|---|---|
| **A. 풀에 있었는데 안 줬다** | 12.2% (1,767) | 8.6% (905) |
| **B. 풀에서 제외 — 작업 중으로 분류** | 41.8% (6,062) | 43.4% (4,581) |
| **C. 아예 안 보임 — GPS 침묵** | 46.1% (6,690) | 48.1% (5,080) |

⇒ **놓친 건의 ~90% 는 "풀에 없어서" 놓친 것이다.** 풀에 있었는데 못 준 건 ~10%.

**그리고 B·C 는 대부분 이미 비어 있던 트럭이다**(TOS `free_ts` 기준·요청 시점에 이미 빈 시간):

| 구분 | 놓친 건 | 이미 빈 시간 중앙 | 2분 넘음 | 10분 넘음 |
|---|---|---|---|---|
| C. 안 보임(침묵 중앙 783초) | 11,571 | 66초 | 46.9% | 32.0% |
| B. staging 으로 분류 | 3,004 | **426초** | 66.3% | 40.4% |
| B. delivering 으로 분류 | 5,079 | 194초 | 54.6% | 22.8% |
| B. empty_travel 로 분류 | 2,411 | 147초 | 52.3% | 26.0% |
| A. 풀에 있었음 | 2,595 | **6초** | 10.9% | 5.3% |

**상태별 커버리지**: `idle` 76.2% · `soon_idle` 60.7% · `wait_rtg` 46.1% / `delivering` 8.4% ·
`staging` 8.9% · `empty_travel` 20.5% · 안 보임 12.9%.
⇒ **트럭을 비었다고 보기만 하면 4번 중 3번은 답이 있다.** 병목은 매칭이 아니라 **"빈 트럭을 빈 줄 모르는 것"**.

**기전(근거 2갈래·가설 아님)**: ①상태가 GPS 파생인데 트럭은 **정지하면 단말이 침묵**한다(기존 확인)
②침묵 트럭을 붙잡는 가지(`soon_idle_held`/`anchored`·우리 추천의 37.6%)는 **"짐을 싣고 드랍 지점
120m 안"** 일 때만 걸린다(`livemap.rs:4755~`) — 즉 **자유로워지는 순간 조건이 깨져 풀에서 빠진다**.
빈 채로 서 있는 트럭이 정확히 사각지대다.

**실행 가능성 — TOS 자유 신호로 풀을 잡을 수 있나**
- `tt_move_log.free_ts` 착지 지연: 중앙 **180~191초** · p90 300 · p99 350초.
- 트럭은 빈 뒤 중앙 **19초** 만에 요청한다 ⇒ **중앙 케이스는 이 신호로 못 잡는다.**
- 그러나 놓친 건의 **35.0%(양하) / 44.1%(적하)** 는 요청 시점에 **이미 자유 신호가 도착해 있었다**.
  그것만 전부 잡으면 커버리지 **21.9 → 49.3% / 19.6 → 55.1%**(상한·같은 창 기준).
- ⇒ 자유 신호는 **보완재**다. 나머지(방금 빈 트럭)는 더 빠른 신호가 필요하다 — `live_workpool` 의
  `ytno` 해제(60초 주기)나 침묵-가지의 '짐 실음' 조건 완화가 후보. **미검증.**

### ⑤ A(풀에 있었는데 안 준 것)의 이유 = 슬롯 부족, 비용 아님 (2026-08-19·2일)

- **틱마다 후보 트럭 63.3대 vs 작업 슬롯 36.3개** → 슬롯은 100% 차고 트럭 27대가 남는다(68%의 틱).
  A 건이 난 그 틱에서 실제로 트럭이 남았음 **양하 97.2% / 적하 91.5%**, 슬롯 전부 찼음 100%.
- **비용은 아니다**: A 트럭 → 그 틱 배정 목적지 중 최근접 거리 중앙 **431m**(배정받은 트럭의 자기 목적지
  607m 보다 가깝다). 배정된 비용 `arrival_s`(자유까지+주행 p50) 최대 1,010초·1,500초 초과 0% —
  간선 상한 1,800초에 닿은 적 없음. 자유까지 시간 = idle 0 · anchored 172 · soon_idle 288 · wait_rtg 345초 중앙.
- 슬롯 규모(3시간 표본·2일): 작업 슬롯 **2~66개/틱** vs 후보 트럭 39~90 vs 3시간 활동 함대 **294~458대**
  vs 다음 1시간 요청 **408~893건**. 미배차 지시는 지금 이 순간 800개(양하 243·적하 557) 있는데
  pool_mode 3(출항 페이스)이 "지금 필요한 만큼"만 담는다 — **push 사고방식**.

### ⑥ 95% 로 가려면 — 사실 4개와 설계 결론

1. **요청의 51.6% 는 트럭이 빈 뒤 30초 안에 온다**(60초 안 54.4%·5분 안 66.9%). 자유 신호를 아무리
   빨리 받아도(원천 로그 33초·조립본 185초) **절반은 못 잡는다** ⇒ **비기 전에 답이 있어야 한다.**
2. 슬롯이 함대의 1/10 (⑤) ⇒ 남는 트럭은 답이 없다. pull 에서 "붙잡아 둔다"는 없다 — 우리가 답을 안 주면
   TOS 가 준다. **트럭을 남기는 것은 결정을 TOS 에 넘기는 것이지 트럭을 아끼는 게 아니다.**
3. **침묵 = 정지**. 정지한 트럭은 마지막 위치에 그대로 있다 ⇒ 마지막 GPS 가 그 트럭의 정확한 위치다.
   30분+ 침묵 트럭이 요청의 8.8%를 낸다 — 코드의 "20분 침묵 = 퇴근" 가정(`SILENT_HOLD_S=1200`)은 틀렸다.
   퇴근 판정은 GPS 가 아니라 **TOS 활동(최근 N시간 배차/자유)** 으로 해야 한다.
4. B(작업 중으로 분류·놓친 건의 42~43%)의 정체: `staging` = 빈 채 정지 + **`latched_*` 가 옛 목표를 붙들고
   있음**(빈 지 중앙 426초) / `empty_travel` = 빈 채 이동(147초) / `delivering` = GPS `container1` 이 늦게
   비거나 도착 미감지(194초). 전부 **실제로는 빈 트럭**이다.

**⇒ 설계 결론: "후보 트럭을 고른다"를 버리고, 근무 중인 모든 트럭에 매 틱 다음 작업을 배정한다.**
- 풀 = TOS 활동이 최근 N시간 안에 있는 모든 트럭(상태 필터 없음). 위치 = 마지막 GPS(정지면 정확).
  자유까지 시간 = idle/빈 채 0 · 싣고 있으면 무브로그 앵커/`learn_cycle_remaining`(그림자 존재·absErr 4~5분).
- 슬롯 = 풀 크기 이상. 출항 페이스는 **버리는 게 아니라 순위**로 쓴다(급한 상자부터 가까운 트럭에).
  1,800초 간선 상한은 폴백으로(먼 트럭도 답은 받게).
- 매 틱 재계산이므로 요청 시점의 답은 ≤60초 전 것 — 그때는 트럭이 거의 빈 상태라 예측 오차가 작다.
- 도달 상한: 함대 식별 실패(2일 488대 중 7대 미출현=1.4%) + 공급 부족 시각 + 계산 실패 ⇒ **~95% 이상 가능**
  (추정·미검증. 지금 커버리지 21.6% → 자유 신호만으로는 62%가 상한이라 이 길밖에 없다).

**리스크(정직하게)**: ①싣고 가는 트럭에 15분 전부터 상자를 붙이면 그 순간의 선택 품질은 낮다 — 단 매 틱
갱신되고 안티스래시가 있어 최종 답은 요청 직전 것이다 ②TOS 가 우리 답을 채택하는 채널이 없는 지금은
커버리지가 올라도 현장 효과 0 ③공급이 얇은 시간대(슬롯 2개 관측)엔 지시 자체가 부족 — 미발행 계획
상자를 낼 수 있는지는 TOS 질문. **구현은 사용자 결정 뒤.**

### ⑦ 근무 중 함대를 실시간으로 특정할 수 있나 (2026-08-19 사용자 질문 · `scripts/fleet_identification.sql`)

**TOS 에 편성표는 없다.** 가장 가까운 `MCH_WORKTIME`(오퍼레이터 로그인 세션)은 **끝난 구간만** 기록된다
(Oracle 실측: 오늘 4,695세션/536대 중 열린 세션 0건 · 최근 60분에 끝난 트럭 119대 vs 실제 활동 450대).
사후 검증용이지 "지금 근무 중" 판정에는 못 쓴다.

**활동 기록으로는 된다** — 규칙 "최근 N시간 TOS 활동(배차 또는 자유)이 있는 트럭 = 근무 중":

| N | 재현율(다음 60분 요청 트럭 중 잡힘) | 잡은 트럭/시각 | 정밀도(잡은 것 중 60분 안 요청) |
|---|---|---|---|
| 1h | 94.2% | 374 | 87.8% |
| 2h | 98.0% | 395 | 86.5% |
| **3h** | **98.4%** | **401** | **85.5%** |
| 6h | 98.6% | 415 | 82.7% |
| + GPS 30분 출현 | **99.0%**(2일 창) | | |

- 근무 세션(3h 공백 기준)은 중앙 **14시간·p90 53시간** — 차량은 교대(00·08·16시 봉우리)를 넘겨 계속 돈다.
  "명단"은 차량 단위로 거의 상수고, 문제는 신규 투입·장기 공백 복귀뿐이다.
- 3h 규칙이 놓친 1.6%: 24h 안 활동 없음(신규/장기 복귀) 35% · 6~24h 전 53% · 3~6h 전 12%.
  이들 중 **72.7%는 요청 10분 전에 GPS 가 켜져 있었다** → 움직이기 시작하면 잡을 수 있다.
- 정밀도 꼬리(잡았지만 60분 안 안 옴 14.5%): 64%는 1~2h 안에 오고, **사실상 퇴근은 20.6%** — pull 에선
  이들에게 슬롯을 준 것이 낭비일 뿐 해는 없다(그 트럭은 묻지 않는다).

**결론: 특정 가능하다.** "TOS 활동 3h ∪ GPS 출현 30분" 이면 요청 트럭의 **99%** 를 미리 명단에 둘 수 있고,
그 명단은 시각당 ~400대. 중간 투입은 GPS 로 잡히고, 퇴근은 정밀도 85%가 흡수한다.
~~슬롯을 400대 이상 확보해야 하는 부담~~ → **사용자 확인(2026-08-19): "요청 상태의 트럭 수 ≤ 배차해야 할
작업 수"는 운영 규칙으로 항상 유지된다**(일이 적으면 근무 차량을 줄인다). 공급 측정은 불필요.
함의: 슬롯을 함대 크기만큼 둘 필요 없이 **자유까지 시간이 짧은 트럭부터 순서대로** 붙인다 — 요청 상태(자유 0)
트럭은 항상 먼저 답을 받고 그 수는 규칙상 작업 수 안이며, 싣고 달리는 트럭은 남는 상자만큼만 미리 붙는다.

### 앞선 판정 중 철회하는 것

- **"우리가 TOS 보다 14~19분 이르다"는 판정 축에서 내린다.** 숫자는 맞지만 pull 구조에서 그 비교는
  의미가 없다. `counterfactual_wait.sql` 의 ② 절은 이제 **상자·크레인 시각을 찾는 기계적 연결고리**로만 쓴다.

## UNKNOWNS

- `stage2_match_shadow` 보존 기간이 7일 이상인지(프룬 설정 미확인) — 첫 질의에서 `min(ts)` 로 확인.
- 적하 `lead_extra_s` 가 최근 행에 채워져 있는지 — NULL 이면 `learn_dispatch_lead` 중앙값으로 대체하고
  그 사실을 표에 적는다.
- 크레인이 실제로 굶은 상자에서는 `comp_ts` 자체가 TOS 배차의 결과라 상한이 과대다 — 그래서 하한을
  같이 낸다. 더 좁히려면 순서 재배열 가정이 필요한데 이번엔 안 한다.

---

## 이번 사이클에서 나온 범위 밖 발견

- **양하 첫 추천의 12%(1,129/9,165)는 TOS 가 우리 첫 추천보다 90초 넘게 먼저 배차한 상자였다**(적하는 2건).
  풀은 시작 시점에 배차된 상자를 빼는데도 이렇다 — 목록 지연·재배차·트윈 중 무엇인지 안 봤다. 양하에만 있다.
- `sql/local/` 은 저장소 루트에 없다(`crates/extractor/sql/local/` 은 include_str 되는 KPI 질의). 분석
  스크립트는 기존 관례대로 `scripts/` 에 뒀다(`deadline_axis_compare.sql` 등과 같은 자리).
- `dispatch_compare_shadow` 에 `(tos_ytno, t1_ts)` 인덱스가 없어 상자↔비교기 짝짓기는 ts 로 먼저 잘라야 한다.

## 이월된 미해소 항목 (지우지 말 것)

- **미해소 래치 경보 `deadman/road_route_eval`** — 마지막 08-11 08:34. 아직 안 봤다.
- **`disk/filesystem` crit** — `/` 98%·여유 24GiB(08-18 실측). root 권한자 몫.
- **`web/public/livemap-roadgraph.geojson` 은 매시 크론이 다시 쓴다** — 항상 modified. 커밋에 딸려
  들어가지 않게 경로를 지정할 것.
- 다 머지된 워크트리 2개(`kc-journal`·`ws-coverage-kc`) — 미머지 0·깨끗. 정리 여부는 사용자 결정.

## 사용자가 답해야 하는 것

- **`DISPATCH_MODE=active` 를 언제 켤 것인가?** 코드 작업은 없다. TOS 소비 채널이 없는 지금은
  얻는 것이 없다.
- (다음 사이클) 이번 측정값에 어떤 **문턱**을 걸어 "시점을 팔 수 있다"로 볼 것인가.
