# HANDOFF — 이번 사이클

마지막 갱신 2026-08-18. 앞선 사이클: 매칭을 착지 신호로 깨운다(`2df856e`, main 푸시됨)
+ 신호 커버리지 감사 갱신(`9d116a1`).

> 상시 사실·기준선·함정은 `~/.claude/notes/tt-aiops-platform.md` 에 있다(136줄).
> 이 파일은 **다음에 무엇을 할지**만 담는다.

---

## GOAL

KC 배차 마감 문서가 "냉동·위험물·OOG 에 사람이 지키는 절차가 있는지는 우리 자료로
확인할 수 없다"고 적어둔 자리에, TOS 운영 DB 에서 실제로 확인한 답이 들어가 있다.

## IN SCOPE

1. **`kc/dispatch/dispatch-deadline.html`** — 2026-08-11 절("냉동·위험물·특수규격도 똑같이
   다룹니다")을 갱신. 결정("빼지 않는다")은 **유지**하고 근거를 보강한다. 담을 것:
   - 배차 자체는 다르지 않다 — 근거 세 갈래(라이브 지시 1,731건 중 섀시·작업우선순위 0건 /
     냉동·OOG 가 일반과 같은 풀 / 배차 풀 설정 154건의 '특별' 표시 전부 빈칸)
   - 절차는 실재하되 배차가 아닌 곳에 있다 — 냉동은 전원 연결·해제(사람 손·지금도 기록),
     위험물은 야드 적치 격리(IMDG 표준표·2010년 적재), OOG 는 표시와 별도 생산성 기준
   - 배차와 맞닿는 지점: 적하 냉동은 전원 해제가 픽업보다 먼저지만(99.4%·중앙 31.9분 전)
     **배차보다는 29% 늦다**(매칭 창을 ±1시간으로 좁히면 39.9%). 양하는 내린 뒤 연결
     (100%·중앙 37.8분·60분 내 61.5%)
   - **분모를 한 문장으로**: 2026-08-15~17 MYT 사흘간 전원 해제 3,155건 중 트럭 작업과
     짝지어진 1,630건(적하 1,033·양하 597)
   - 준비시간 차이는 **측정만** 적고 원인은 미상으로 문장을 나눈다: 적하 위험물 14.0분 vs
     일반 10.4분(n=631/25,708), 양하 OOG 15.7분 vs 8.3분(n=82/39,187)
   - 남은 미확인 하나를 남긴다: 전원 해제가 늦어 트럭이 실제로 기다리는지는 아직 안 쟀다
2. **`kc/data/tos-verification.html:71`** — 항목 E "특별취급(냉동·위험물·OOG) 제약 ⏸️ 보류 —
   롤아웃 시 제외"가 지금 사실과 어긋난다(08-11 에 "빼지 않는다"로 결정됨). **한 줄만** 고친다.
3. ~~**`CLAUDE.md`** — 6일째 미커밋인 scengen 이관 문단을 커밋한다.~~ → **이번 사이클 중
   다른 손이 처리했다**(`4b7dfaa`, 2026-08-18 14:00). 이 워크트리는 공유된다 — 남이 같은 시간에
   커밋할 수 있음을 전제로 일할 것.

KC 규약대로 쉬운 말로 쓰고 **테이블·컬럼 이름은 노출하지 않는다**
(예: 전원 로그 → "냉동 컨테이너 전원을 연결하고 끊은 기록").
숫자는 요약문을 재인용하지 말고 측정 출력에서 다시 읽어온다.

## OUT OF SCOPE

배차 로직·코드 일절 변경 없음. 추가 측정 없음(냉동 대기 측정도 안 함). ETW 순서 측정 안 함.
scengen 워크트리의 미커밋 `assemble.rs` 안 건드림. 다 머지된 워크트리 2개(`kc-journal`,
`ws-coverage-kc`) 정리 안 함. **Oracle 추가 조회 없음** — 이미 얻은 값만 쓴다.

## DONE CRITERIA

```bash
grep -c "이 방법으로는 확인할 수 없습니다" kc/dispatch/dispatch-deadline.html   # 기대 0
grep -c "전원" kc/dispatch/dispatch-deadline.html                              # 기대 1 이상
grep -c "보류" kc/data/tos-verification.html                                   # 항목 E 가 사라졌는지
curl -s -o /dev/null -w '%{http_code}\n' localhost:<포트>/kc/dispatch/dispatch-deadline.html  # 기대 200
git status --short CLAUDE.md        # 기대: 출력 없음
git log -1 --format=%s -- CLAUDE.md # 기대: 이번 커밋 제목
```
⚠ API 포트는 유닛 파일에서 확인해 맞춘다. 공유 워크트리라 커밋은 `git commit -m "..." -- <경로>`
로 경로를 지정한다(`--` 는 `-m` 뒤).

## UNKNOWNS

- **전원 해제가 늦어 트럭이 실제로 기다리는가** — 이번엔 안 잰다. 재려면 냉동↔일반으로 갈라
  야드 대기시간을 봐야 하고, GPS 도착 앵커(`truck_pos_hist`)가 2일치라 창이 좁다.
  ⚠ "해제가 배차보다 늦었는가"로 층화하면 안 된다 — 해제가 픽업에 붙어 있어
  `픽업−배차 ≥ 32분`과 거의 같은 말이 되는 동어반복이다.
- 양하 연결 p90 945분 꼬리가 진짜인지 매칭 잡음인지 — 문서에는 p90 을 적지 않는다.
- TOS 에 안 남는 사람 규칙(예: 위험물 배차 시 무전 승인)은 여전히 확인 불가.

---

## 다음 후보 (앞 사이클에서 이월)

1. **★ETW 가 작업목록 착지 뒤에 온다 — 측정 후 판단**(사용자 ㉠ 결정으로 미룬 것).
   추출기가 `workqueue → workpool → etw` 순이라 WORKPOOL 착지가 ETW 단계 **전에** 커밋된다.
   옛 `:15` 위상에서는 틱이 착지 ~20초 뒤라 ETW 가 대개 끝난 뒤였는데, 지금은 0~2초 뒤다.
   ⇒ **먼저 잴 것**: 착지 직후와 20초 뒤의 값 차이, 그리고 그 차이가 `work_eta` 를 얼마나 움직이는가.
2. **`tt-workpool` 이 1분 주기를 못 지키는 것** — 평균 60.3초. ETW 를 별도 유닛으로 떼면
   본체가 20초대로 내려갈 것으로 보이나(가설), ETW 는 공유 인프라라 경계 확인이 먼저다.
3. **TOS 기술 세션** — `docs/tos-integration-handoff.md` 의 7개 질문. 기술 쪽 유일한 크리티컬 패스.
   ★이번 조사로 "특별취급 절차" 질문은 **무전 승인 같은 기록 안 남는 규칙 하나로 좁혀졌다.**
4. **운영자 채택/기각 기록 장치** — 파일럿 Phase 1→2 통과 기준이 "운영자 수용"인데 재는 장치가 없다.
5. **비교기 지표 재측정** — T1 절체(`t1_ver=1`) 후 평시 표본이 쌓였을 것이다.
6. **한산한 시간대 DEADMAN 오경보 해소** — `stage2_match_shadow` 가 "지금 할 일이 있는가"를 안 본다.
7. **평문 비밀번호 정리** — `scripts/*.sh` 의 `PGPASSWORD=wp`. GitHub 원격이 있다.
8. **디스크 root 영역** — 08-09 에 여유 20GiB 밑까지 갔다. root 권한자 몫.
9. **다 머지된 워크트리 2개 정리**(`kc-journal` 336e386 · `ws-coverage-kc` 9d116a1) — 둘 다
   main 에 미머지 커밋 0·작업트리 깨끗이라 지워도 유실 없다. ~~scengen 워크트리 미커밋분~~은
   2026-08-18 에 `089f88a` 로 커밋되고 `c100919` 로 main 에 머지됐다.

## 이월된 미해소 항목 (앞 사이클에서 옮겨 적음 — 지우지 말 것)

- **미해소 래치 경보 `deadman/road_route_eval`** — 마지막 08-11 08:34. 아직 안 봤다.
  (`disk/filesystem` 은 마지막 08-09, 후보 8번과 같은 건이다.)
- **`web/public/livemap-roadgraph.geojson` 은 매시 크론이 다시 쓴다** — `git status` 에 **항상**
  modified 로 뜬다. 저장소가 더럽다고 판단하는 근거로 쓰지 말고, 커밋에 딸려 들어가지 않게
  경로를 지정할 것.

## 사용자가 답해야 하는 것

- **`DISPATCH_MODE=active` 를 언제 켤 것인가?** 코드 작업은 없다. 켜는 순간의 유일한 효과는
  "직전 180초에 우리가 추천한 상자를 풀에서 뺀다"이고, TOS 소비 채널이 없는 지금은 얻는 것이 없다.
- ~~TOS 에 특별취급 절차를 물을 것인가~~ → **이번 조사로 대부분 닫힘.** 배차 경로에는 특별취급이
  없다(근거 3갈래). TOS 에 물을 것은 "기록에 안 남는 사람 규칙이 있는가" 하나로 좁혀졌다.
