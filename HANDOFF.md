# HANDOFF — 다음 사이클

마지막 갱신 2026-08-26. 앞선 사이클(트럭 축 진단·조사만·배차 로직 변경 0):
**"적하 트럭이 배차 순간 명단에서 22% 빠진다"는 분모 오류였다.** 트럭이 새로 빈 순간만 세면
커버리지는 **적하 96.3 / 양하 95.5%**. 다만 그 96%의 68%는 "미리 배정"(2계층)이고
**1계층 기준은 27.5%**, **TOS 가 준 그 작업 지목은 4.8%** 다.

> 상시 사실·기준선·함정은 `~/.claude/notes/tt-aiops-platform.md`(150줄 상한). 이 파일은 **다음에 할 일**만.

---

## 지금 참인 것 (전에는 아니었던 것)

- **성적의 분모가 정해졌다**: `scripts/ld_pool_absence.sql`(읽기 전용·6초). 비교기 사건을
  "직전 사건 이후 자유 사건이 있었나"로 갈라 **A(새 요청)만** 성적에 쓴다. 자유 정답지는 원천 드랍
  로그(적하 `qc_move_log` · 양하 `tos_handover_label`). 비교기 사건의 **35%가 A 가 아니다.**
- **계층을 반드시 가른다**: 계층 불문 커버리지는 사실상 "명단에 있었나"에 가깝다. `mig0161` 이
  이미 "종전 비교시 2계층을 거를 것"이라 못박아뒀는데 이번에 그걸 어겨서 발견했다.
- **⓪절 가드**가 매 실행 원천 8개의 **보관 기간과 착지 신선도**를 검사한다. 자유 원천이 멈추면
  그 구간이 통째로 B 로 몰려 이 분석의 논지가 무너지므로, 조용한 실패를 막는 장치다.
- **KC 갱신**: `kc/dispatch/index.html` §8 에 "성적을 말할 때 같이 밝혀야 하는 두 가지" 추가.
  기존에 적혀 있던 "추천 걸린 비율 ≈ 4분의 1"과 이번 1계층 27.5% 가 같은 값임을 연결해 뒀다.

## 일부러 안 한 것

- **고치지 않았다.** 조사 지시라 원인만 밝혔다. 배차 로직·풀 구성·비교기 전부 그대로다.
- **양하 B2 의 정체** — 독립 반증이 빗나갔다(적하는 예측대로). 적하와 다른 무언가가 있는데 안 팠다.
- **함대 밖 갈래** — 별도로 재지 않았다. "위치 기록이 아예 없는" 몫과 겹칠 수 있으나 미분리.

## 다음 후보 (한 줄 근거)

1. **★합격 기준을 어디에 둘지 정하기** — 1계층 기준 27.5% 를 목표선으로 삼을지, "미리 배정"도
   답으로 칠지. 이걸 정해야 나머지가 다 정해진다. **사용자 결정 사항**(아래 참조).
2. **작업 축 4.8%** — 같은 순간 같은 일을 고르는 비율. 트럭은 준비돼 있으나 무엇을 시킬지가
   안 맞는다. 99% 목표가 트럭 축이라면 이미 96%이고, 진짜 여지는 여기다.
3. **비교기에 판별자 달기** — `dispatch_compare_shadow` 는 "새로 비지 않은" 배차를 계속 새 사건으로
   담는다. 판별자 컬럼을 달아 소비처가 거를지, 분석 때만 가를지. 화면·문서 어디에도 새 요청 기준
   수치가 없다.
4. **남은 3~4%p** — 절반이 위치 행 3h 초과 트럭. ⚠상한을 재현율만 보고 올리지 말 것(1~3h 오차 974m).
   양하는 놓침의 27.8%가 "몇십 초 늦었을 뿐"이라 적하와 처방이 다를 수 있다.
5. **B 안의 재배정 몫** — 빈 차로 픽업하러 가는 트럭에 답을 줄지. 재지향(pool_ver 8) 설계와 겹친다.
6. **TOS 기술 세션 준비**(`docs/tos-integration-handoff.md` 7문) · **운영자 채택/기각 기록 장치**.
7. **잡무**: 평문 비밀번호(PGPASSWORD=wp·GitHub 원격) / 디스크 / 머지된 워크트리 2개
   (`kc-journal`·`ws-coverage-kc` — 둘 다 main 에 병합 확인됨·⚠kc-journal 은 kc-keeper 소유일 수 있음).

## 이월된 미해소 항목 (지우지 말 것)

- **(1차 리뷰 CONSIDER·08-25) 재지향 판별의 야드 작업 사각**: 픽업 가드가 DS/LD 로그만 봐서 MI/MO 로
  적재 중 + 선배정 Q 인 트럭이 통과 가능(크기 미측정). GPS 적재 라벨 보조는 과잉 제외 위험(98.7→87.7% 전례).
- **(〃) 재지향 트럭 anti-thrash 소멸** · **(〃) 표시 비대칭**(redirectable 낡은 GPS held 미포함).
- **no_coord** — 주간 최대 58·야간 0~3(08-25~26 실측). 잔여 놓침의 주원인은 **아님이 확인됨**.
- **`tt-weather` 단발 실패**(08-21·08-25 재발·재시도 여부 사용자 판단). ⚠`ops_alert`는 MYT·저널은 KST.
- **`web/public/livemap-roadgraph.geojson` 은 매시 크론이 다시 쓴다** — 항상 modified·커밋 경로 지정.
- `dispatch_compare_shadow` 에 `(tos_ytno,t1_ts)` 인덱스 없음 · `stage2_solver_shadow` DEADMAN 밖.
- **⚠오염 창 08-25 15:20~15:41 MYT 의 1계층 switched 는 시계열 비교에서 제외**(prev 버그 구간·수정 완료).
- 화면 해석 문의 대비: 보드 "미리" 칩 · 보드 작업 수 245대(2계층 편입) · QC active_moves 이른 점등(무해).

## 사용자가 답해야 하는 것

1. **★합격 기준** — 트럭 축 99% 는 이미 96%다. 남은 선택지: (a) 1계층 기준 27.5% 를 목표선으로
   재정의 (b) 작업 축(그 작업 지목 4.8%)으로 목표를 옮김 (c) 현행 유지. **이게 다음 사이클의 주제를 정한다.**
2. (e) tt-handover 확장 시점 — 변화 없음.
