# HANDOFF — 다음 사이클

마지막 갱신 2026-08-26. **다음 사이클 주제는 사용자가 정했다(아래 ★).**

앞선 사이클(트럭 축 진단·조사만·배차 로직 변경 0): "적하 트럭이 배차 순간 명단에서 22% 빠진다"는
**분모 오류**였다. 트럭이 새로 빈 순간만 세면 커버리지는 **적하 96.3 / 양하 95.5%**.
다만 그 96%의 대부분은 "미리 배정"이고 **지금 보낼 때가 된 일 기준은 22~28%**,
**TOS 가 준 그 작업 지목은 4.8%** 다. 상세 = `docs/cycles/2026-08-26-ld-pool-absence.md`.

> 상시 사실·기준선·함정은 `~/.claude/notes/tt-aiops-platform.md`(150줄 상한). 이 파일은 **다음에 할 일**만.

---

## ★ 다음 사이클 — "우리가 늦은 건가, TOS 가 이른 건가"

### 왜 이걸 하나 (사용자 지시 2026-08-26)

트럭이 일을 달라고 한 순간, 우리가 "지금 보낼 때가 됐다"고 본 것은 **22~28%뿐**이고 나머지는
"아직 때가 아니다"였다. **그런데 TOS 는 바로 그 순간 실제로 트럭을 보냈다.** 둘 중 하나다:

- **(A) TOS 가 너무 일찍 보낸다** → 트럭이 크레인 앞에서 기다리고 있을 것이다. 그게 우리가 고치려는 낭비다.
- **(B) 우리 마감이 너무 늦다** → 우리 말대로 하면 크레인이 트럭을 못 받아 논다.

**어느 쪽인지 한 번도 안 재봤다.** 현장에 배차를 넘기기 전에 반드시 답해야 하는 질문이다.

### 왜 이 값이 낮은지는 이미 안다 (그러니 여기부터 다시 조사하지 말 것)

지금 이 순간 **출발시킬 일 자체가 트럭보다 훨씬 적다**(최근 6h 실측: 틱당 트럭 263대 · 지금 보낼
일 43개 · 미리 배정용 154개 / 우리가 낸 추천의 22%만 "지금 출발"). 설계가 의도한 결과다 —
"시점이 곧 지시다"라서 일찍 보내면 트럭이 크레인 앞에 선다. **문제는 이 값이 낮다는 게 아니라
그 판단이 옳은지 모른다는 것이다.**

### 이미 있는 도구 (새로 만들지 말 것)

- **`scripts/counterfactual_wait.sql`** — "우리 추천대로 보냈다면 트럭이 크레인 앞에서 몇 분
  기다렸을까". ①헤드라인(대기 상한/하한) ②같은 상자를 TOS 와 나란히(준비시간 모델과 무관한 값)
  ③**위약 내장**(TOS 자기 배차에 같은 잣대를 대면 대기 중앙이 0 부근이어야 잣대가 맞다).
  **2계층 게이트 이미 적용됨**(`match_tier IS DISTINCT FROM 2`·64·101행). 스크립트 머리말의
  ⚠정정 2건과 한계 절을 **먼저 읽을 것**.
- (A) 방향은 위 도구가 덮는다. **(B) 방향(크레인 굶김)은 별도 계기가 필요하다** — `/api/stage2`
  경로에 `starving_real`·`near_idle_tt` 게이지가 있고, 종전 실측은 "굶은 건 8.6%뿐"이었다.
  두 방향을 **같이** 봐야 한다. 한쪽만 보면 반대편으로 최적화된다.

### 함정 (이 조사에서 틀리기 쉬운 것)

- ⚠**"지금 보낼 일 비율(22~28%)"을 목표로 잡고 마감을 앞당기면 숫자는 바로 오르지만 그게 곧
  트럭을 일찍 보내는 것**이다. 이 값을 쓰려면 **반드시 대기 시간과 나란히** 볼 것.
- ⚠**TOS 배차시각(`dispatch_ts`)은 "TOS 가 결정한 시각"이 아니라 트럭이 빈 시각**이다. "우리가
  TOS 보다 몇 분 이르다"류 비교는 2026-08-19 에 이미 철회됐다. 판정은 **결과(대기·굶김)로** 한다.
- ⚠**TOS 와 얼마나 같은가를 합격 기준으로 잡지 말 것**(규약 CLAUDE.md:16). 작업 지목 4.8% 는
  목표가 아니라 "우리가 TOS 와 얼마나 다르게 판단하나"를 보는 **계기**다.
- ⚠성적을 말할 때는 **무엇을 셌는지 함께** 밝힌다(`scripts/ld_pool_absence.sql` ①②-b절·KC 갱신됨).

## 그 밖의 후보

1. **비교기에 판별자 달기** — `dispatch_compare_shadow` 는 "트럭이 새로 비지 않은" 배차를 계속 새
   사건으로 담는다(35%). 판별자 컬럼을 달아 소비처가 거를지, 분석 때만 가를지. 화면·문서 어디에도
   새 요청 기준 수치가 없다.
2. **남은 3~4%p** — 절반이 위치 행 3h 초과 트럭. ⚠상한을 재현율만 보고 올리지 말 것(1~3h 오차 974m).
   양하는 놓침의 27.8%가 "몇십 초 늦었을 뿐"이라 적하와 처방이 다를 수 있다.
3. **양하 B2 의 정체** — 독립 반증이 빗나갔다(적하는 예측대로). 적하와 다른 무언가가 있다.
4. **B 안의 재배정 몫** — 빈 차로 픽업하러 가는 트럭에 답을 줄지. 재지향(pool_ver 8) 설계와 겹친다.
5. **TOS 기술 세션 준비**(`docs/tos-integration-handoff.md` 7문) · **운영자 채택/기각 기록 장치**.
6. **잡무**: 평문 비밀번호(PGPASSWORD=wp·GitHub 원격) / 디스크 / 머지된 워크트리 2개
   (`kc-journal`·`ws-coverage-kc` — 둘 다 main 에 병합 확인됨·⚠kc-journal 은 kc-keeper 소유일 수 있음).

## 이월된 미해소 항목 (지우지 말 것)

- **(08-25 리뷰 CONSIDER) 재지향 판별의 야드 작업 사각**: 픽업 가드가 DS/LD 로그만 봐서 MI/MO 로
  적재 중 + 선배정 Q 인 트럭이 통과 가능(크기 미측정). GPS 적재 라벨 보조는 과잉 제외 위험(98.7→87.7% 전례).
- **(〃) 재지향 트럭 anti-thrash 소멸** · **(〃) 표시 비대칭**(redirectable 낡은 GPS held 미포함).
- **함대 밖 갈래 미분리** — "위치 기록이 아예 없는" 몫과 겹칠 수 있으나 안 갈랐다(08-26).
- **no_coord** — 주간 최대 58·야간 0~3. 잔여 놓침의 주원인은 **아님이 확인됨**(08-26).
- **`tt-weather` 단발 실패**(08-21·08-25 재발·재시도 여부 사용자 판단). ⚠`ops_alert`는 MYT·저널은 KST.
- **`web/public/livemap-roadgraph.geojson` 은 매시 크론이 다시 쓴다** — 항상 modified·커밋 경로 지정.
- `dispatch_compare_shadow` 에 `(tos_ytno,t1_ts)` 인덱스 없음 · `stage2_solver_shadow` DEADMAN 밖.
- **⚠오염 창 08-25 15:20~15:41 MYT 의 1계층 switched 는 시계열 비교에서 제외**(prev 버그 구간·수정 완료).
- 화면 해석 문의 대비: 보드 "미리" 칩 · 보드 작업 수 245대(2계층 편입) · QC active_moves 이른 점등(무해).

## 사용자가 답해야 하는 것

- **합격 기준 재정의는 위 ★ 결과가 나온 뒤로 미룬다**(사용자 판단 2026-08-26). 트럭 축 99% 는 이미
  96%라 달성됐고, "지금 보낼 일 기준 22~28%"를 목표선으로 쓸지는 **그 값이 옳은지 안 뒤에** 정한다.
- (e) tt-handover 확장 시점 — 변화 없음.
