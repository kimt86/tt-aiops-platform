# HANDOFF — 이번 사이클

마지막 갱신 2026-08-26. 앞선 사이클(발행 2계층·mig0161·전부 main)은 끝났다: none(작업 축) DS 59.0→29.5 /
LD 67.9→32.4%. 이어서 **/research 로 남은 몫을 진단**한 결과 **질문 자체가 두 축이었음**이 드러났고,
사용자가 **트럭 축**을 택했다. 이번 사이클은 그 축의 1순위 원인을 **조사만** 한다(고치지 않는다).

> 상시 사실·기준선·함정은 `~/.claude/notes/tt-aiops-platform.md`(150줄 상한). 이 파일은 **다음에 할 일**만.

---

## 이번 사이클 (2026-08-26 · 조사)

### GOAL
적하 트럭이 TOS 배차 순간 우리 후보 명단에 없는 **22%가 무엇 때문인지 이름 붙은 항목으로 나뉘고**,
각 항목의 크기와 분모가 확정된다.

### 출발점 측정 (/research 2026-08-26 · 근거)
- **트럭 축 커버리지** = TOS 가 배차한 트럭에 그 순간 우리 유효 추천(150초)이 서 있던 비율:
  **DS 82.0%(n=4,377) / LD 77.3%(n=12,485)**. 분모 = 08-25 15:45~08-26 08:10 MYT 에 비교기가 채점한
  TOS 배차 사건, **(트럭, 배차시각) 단위 중복 제거**(t1_ver=1·reco_src NOT NULL·DS/LD). 시간대별
  73.7~85.2% 로 안정(야간 회귀 없음).
- **빈 곳의 구성**: 트럭이 **명단에 아예 없음** LD **22.0%** / DS 10.6% · **재지향이라 슬롯 제외**
  DS 7.4% / LD 0.6%. ⇒ **명단에 든 트럭은 사실상 전부 추천을 받는다 — 병목은 추천 로직이 아니라 명단.**
- **작업(큐) 축과 섞지 말 것**: none 사건의 71~81% 는 **그 트럭에 추천이 있었고 다른 큐를 가리킨 것**.
  트럭·큐가 둘 다 맞은 경우는 전체의 3~5% 뿐(DS 235/4,748 · LD 386/12,465).
- 배제됨(주원인 아님): 미러 누락(Oracle 미배차 532 = 우리 532, 정확히 일치) · 좌표 없어 탈락(야간 0~3건)
  · 마감 없어 탈락(0건·with_deadline = works 전량).

### ⚠ 첫 갈래는 시계 분리다 (이걸 먼저 하지 않으면 원인 귀속이 틀린다)
T1 = `yt_dis_ts` 는 **트럭이 빈 시점이지 TOS 결정 시점이 아니다**(기존 확정 사실). 트럭이 실제로 물어보는
것은 **빈 뒤 35~45초**. 그래서 같은 현실에 두 수치가 공존한다 — 요청 순간 기준 ⑮ 풀 재현율 **96.4%** vs
빈 순간 기준 명단 포함 **78%(적하)**. **22% 중 얼마가 "몇십 초 늦었다"인지**가 첫 갈래이며, 크면 남은
진짜 결함은 22% 보다 훨씬 작다. 좁은 창 표본(적하 36건)은 T1 이후 명단 등장 10건·지연 중앙 123초,
나머지 26건은 30분간 미등장 — **표본이 작아 전체 창으로 다시 재야 한다.**
목표는 그대로 둔다: 규약이 "시점이 곧 지시다"이고 **비기 전에 답이 있어야 한다**는 요구는 이미 확정돼 있다.

### IN SCOPE
- 새 진단 스크립트 `scripts/ld_pool_absence.sql`. 모집단 = pool_ver 8 창의 **적하** 배차 사건 중
  T1 에 추천도 명단 기록도 없던 것(16h 창 약 2,747건). **양하(10.6%)도 같은 분해를 대조군으로** 돌려
  적하 고유인지 공통인지 가른다.
- **다섯 갈래 상호배타 귀속**:
  1. **시계 차이** — 요청 순간(`assigned_tt_hist` 첫 등재)에는 명단에 있었나 · T1↔요청 간격 분포.
  2. **일하는 중** — T1 에 상자를 싣고 있었나(**마지막 픽업 > 마지막 자유**). 적하가 픽업 직후 A/Q 에서
     사라지는 알려진 사각과 겹치는지.
  3. **예측 꼬리** — 일하는 중이었다면 "곧 빔" 예측은 뭐라 했나(지평 밖 / 예측 없음).
  4. **위치 탈락** — 위치 없음 · 나이 상한 초과.
  5. **함대 밖** — 근무 함대 판정 밖.
- 사이클 문서 `docs/cycles/2026-08-26-ld-pool-absence.md`: 항목별 수치 · **분모 한 문장씩** · 최대 지렛대.

### OUT OF SCOPE
- **배차 로직·풀 구성 변경 없음.** 원인이 밝혀져도 이번엔 고치지 않는다(조사 지시).
- 작업(큐) 축 none 30% 와 **1:1 절단**(미배차 후보 300~470 중 트럭 수만큼만 발행·매 틱 40~50% 절단·
  큐 42개 중 9개 미커버) — 이번 축이 아니다. 별도 사이클 후보로 남긴다.
- 재지향 트럭 슬롯 제외(DS 7.4%) — 의도된 설계라 별건.
- ⑮ 재현율 정의 변경 · TOS 쓰기 · 프론트.

### DONE CRITERIA
```bash
psql -h 127.0.0.1 -p 5433 -U wp -d wp_tt -f scripts/ld_pool_absence.sql
```
- 적하 "명단 없음" 사건이 5항목으로 **상호배타적으로** 나뉘고 **미분류 ≤ 5%**.
- 항목마다 **분모를 한 문장으로** 쓸 수 있다.
- **안정성**: 최근 6시간 창으로 좁혀도 **상위 2개 항목의 순서가 바뀌지 않는다**.
- **위약**: 1순위 원인에 반증을 한 번 시도해 통과(무엇으로 반증했는지 문서에 남긴다).
- 양하 대조군 분해가 같이 나와 **적하 고유인지 공통인지 한 문장으로 판정**된다.
- **이번 사이클은 22% 를 줄이지 않는다** — 설명하는 것이 산출물이다.

### ⚠ 보관 창 (지금 돌려야 하는 이유)
`truck_pos_hist` **2일** · `stage2_pool_truck_shadow` **3일** · `assigned_tt_hist` **3일**.
지금 창(08-25 15:45~)이 딱 맞아떨어진다. 늦어지면 뒷부분부터 잘린다 — 필요하면 먼저 스냅샷을 뜬다.

### 지켜야 할 잣대 (이 조사에서 틀리기 쉬운 것)
- **요청 시각은 `assigned_tt_hist`(자유 뒤 첫 등재)로 잰다.** `tt_move_log.dispatch_ts` 는 최종 배차만
  남겨 재배정된 첫 배차가 사라진다 — 요청 시각 분석에 쓰지 말 것.
- **자유 시각 정답지는 `tt_move_log.free_ts`(TOS)**. GPS `dropped_at` 은 33% 놓쳐 채점에 못 쓴다.
- **GPS 상태 라벨은 보조로만**(빈 판정 금지·staging 426s/delivering 194s). 앵커 픽업에 `status='F'` 금지.
- **정확도 규칙과 소속 규칙을 같은 분기에서 처리하지 마라**(전례: 재현율 98.7→87.7%).
- **분모가 결과에서 파생됐는지** 확인 — 층화변수↔결과 부호 일치율 + 위약 층화.

---

## 다음 후보 (이번 사이클 뒤)

1. **적하 명단 누락 해소** — 이번 조사에서 1순위 원인이 정해지면 그것을 고친다(비기 전에 명단 진입).
2. **1:1 절단** — 작업 축 커버리지의 구조적 상한. 트럭 한 대에 작업 하나라 미배차 후보의 40~50%가
   매 틱 잘리고 큐의 ~21%가 그 순간 통째로 빠진다. 트럭 축과 다른 레버가 필요하다.
3. **⑮(요청 순간)와 T1(빈 순간) 두 잣대 정리** — 같은 사건 집합에 두 정의를 모두 적용해 관계를 확정하고,
   화면·문서의 합격 기준을 하나로 못 박는다.
4. **VS TOS 화면 ② 전환 + reco_* 24h 재집계** · **재지향 관찰**(redirected_from 이 정답지) ·
   **TOS 기술 세션 준비**(`docs/tos-integration-handoff.md` 7문) · **운영자 채택/기각 기록 장치**.
5. **잡무**: 평문 비밀번호(PGPASSWORD=wp·GitHub 원격) / 디스크 / 머지된 워크트리 2개
   (`kc-journal`·`ws-coverage-kc` — **둘 다 main 에 완전 병합 확인됨**·⚠kc-journal 은 kc-keeper 소유일 수
   있어 확인 후 정리).

## 이월된 미해소 항목 (지우지 말 것)

- **(1차 리뷰 CONSIDER·08-25) 재지향 판별의 야드 작업 사각**: 픽업 가드가 DS/LD 로그만 봐서 MI/MO 로
  적재 중 + 선배정 Q 인 트럭이 통과 가능(크기 미측정). GPS 적재 라벨 보조는 latched 잔류로 과잉 제외
  위험(98.7→87.7% 전례) — **측정 먼저**. ⚠이번 조사의 갈래 2 와 겹칠 수 있다.
- **(〃) 재지향 트럭 anti-thrash 소멸**: 상수 벌점은 버킷 간 상대 순서 불변 → 틱마다 튈 수 있음.
- **(〃) 표시 비대칭**: redirectable 낡은 GPS held 미포함(need_pos 경로는 포함)·TT페이지 free_in 0 라벨.
- **no_coord 22.9/틱(~20%)** 이라던 값은 **현행 관측과 다르다** — 08-25~26 실측은 주간 최대 58·야간 0~3.
  원인 미조사이나 **잔여 none 의 주원인은 아님이 이번에 확인**됐다.
- **`tt-weather` 단발 실패**(08-21·08-25 재발·재시도 여부 사용자 판단). **08-25 13:53 MYT 추출기 3유닛
  단발 warn**(rtg/qc-moves·handover·2계층 배포 전) — 재발 시 조사. ⚠`ops_alert`는 MYT·저널은 KST.
- **`web/public/livemap-roadgraph.geojson` 은 매시 크론이 다시 쓴다** — 항상 modified·커밋 경로 지정.
- `dispatch_compare_shadow` 에 `(tos_ytno,t1_ts)` 인덱스 없음(이번 조사에서 반복 조회하므로 체감된다)
  · `stage2_solver_shadow` DEADMAN 밖.
- **GPS 죽었는데 TOS 는 배차하는 트럭**(위치 나이 중앙 4.3h) — 재현율 3.2%p 상한·위치 원천 판단 대기.
- **⚠오염 창 08-25 15:20~15:41 MYT 의 1계층 switched 는 시계열 비교에서 제외**(prev 버그 구간·수정 완료).
- 화면 해석 문의 대비: 보드 "미리" 칩(08-25 신설) · 보드 작업 수 245대(2계층 편입) ·
  BoardFunnel issued 08-24 점프 · QC active_moves 이른 점등(무해).

## 사용자가 답해야 하는 것

- (e) tt-handover 확장 시점 — 변화 없음.
