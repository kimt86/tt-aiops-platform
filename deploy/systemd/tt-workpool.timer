[Unit]
Description=wp-tt LIVE work-pool snapshot every 60s (one merged pool_tick pull; CHUNK8)

[Timer]
OnBootSec=1min
# :15 — clear of the 60s move triad (:05/:25/:45) and vessel-schedule (:35).
OnCalendar=*-*-* *:*:15
AccuracySec=1s

[Install]
WantedBy=timers.target
