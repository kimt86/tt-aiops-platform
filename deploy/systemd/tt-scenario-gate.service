[Unit]
Description=tt scenario landside gate transactions (ISOLATED, non-critical): CYC_HISTORY gate intake/exit for the GI/GO containers already in the local yard stream -> scenario.gate_event. Fault here never affects dispatch/dashboard extraction.
After=network-online.target

[Service]
Type=oneshot
EnvironmentFile=%h/projects/tt-aiops-platform/.env
ExecStart=%h/projects/tt-aiops-platform/target/release/scengen gate --target oracle-prod
Nice=19
