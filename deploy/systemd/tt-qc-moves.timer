[Unit]
Description=wp-tt quay-crane move stream every 1min (incremental MCH_OPERATION C/M/Z; SEQNO PK-seek watermark)

[Timer]
OnBootSec=90s
OnCalendar=*-*-* *:*:05
AccuracySec=1s

[Install]
WantedBy=timers.target
