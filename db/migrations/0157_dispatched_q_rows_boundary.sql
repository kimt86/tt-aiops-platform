-- 0157 — 배차된 Q행 착지 경계 기록 (2026-08-24 · 브랜치 dispatched-q-rows)
--
-- 코드 변경(추출기 착지 match 한 갈래)이 표 셋의 **모집단**을 바꾼다. 스키마 변경은 없고
-- 경계를 판별자·COMMENT 로 남긴다. 전부 멱등.
--
-- 무엇이 바뀌나: TOS 는 배차를 상자 행에 즉시 찍는데(JOBSTATUS='Q' + YTNO + YT_DIS_DT),
-- 종전 추출기는 이 행을 A 갈래(jobstatus='A')에도 Q 갈래(ytno 빈 것)에도 안 실어 버렸다.
-- 그래서 live_workpool 에 배차가 픽업 후 'A' 전환 때야 나타났고(양하 탐지 p50 544초),
-- 이제 배차 ≤60초(추출 주기)에 나타난다. 실측 순간값: 버려지던 행 116(DS 77·LD 39).

-- ① 풀 판별자 — 코드 상수(livemap.rs POOL_VER)가 7 로 올라간다. 경계 서술 동기화.
COMMENT ON COLUMN stage2_pool_truck_shadow.pool_ver IS
  '풀 규칙 판. 집계는 반드시 이 값으로 가른다 — 판이 다르면 모집단이 다르다(livemap.rs POOL_VER). '
  '7 = 추출기가 Q+트럭(배차됨·픽업 전) 행을 live_workpool 에 착지(2026-08-24) — 배차 신호가 '
  '≤60초에 도착해 배차된 트럭이 풀에서 더 일찍 빠진다. 6 이하 경계는 mig0156·livemap.rs 참조.';

-- ② 비교기 — 모집단 확장 경계. 종전에는 배차행이 픽업 후에야 비교기에 들어와 first_seen−t1_ts
--    가 양하 p50 544초였다. 이후로는 Q+트럭 행이 바로 들어와 지연이 추출 주기(≤~120초)로 준다.
--    탐지 지연·일치율을 경계 너머로 이어 읽으면 두 모집단이 섞인다.
COMMENT ON TABLE dispatch_compare_shadow IS
  'TOS 배차 vs 우리 선택, T1 시점 재구성 비교(그림자). '
  '⚠2026-08-24 경계(브랜치 dispatched-q-rows·새 모집단 첫 착지 04:06:55Z·배포는 extractor 먼저 '
  '→ api(POOL_VER 7) 04:07:14Z 재시작·ver7 첫 틱 04:08:03Z): 이전에는 배차행이 픽업 후(A 전환) '
  '에야 들어와 탐지가 양하 p50 544초 늦었고, 이후로는 Q+트럭 행이 배차 ≤60초에 들어온다. '
  '⚠탐지 지연을 잴 때 분모 규칙 둘: ①경계는 ts(착지)가 아니라 t1_ts(배차 시각)로 걸러라 — '
  'ts 로 거르면 배포 순간의 미픽업 재고(~116행·배차는 과거)가 544초급 지연으로 p50 을 오염시킨다. '
  '②(qc,queuename,tos_ytno,t1_ts) 단위 min(ts) 로 접어라 — UPD_DT 갱신 재비교(47%가 2행+, mig0152)가 '
  '늦은 first_seen 을 섞는다. t1_ver/t1_ts 는 mig0149·0152.';

-- ③ 공정비교 15분 창 — 같은 확장을 받는다(질의가 live_workpool 배차행을 읽음).
COMMENT ON TABLE fair_compare_shadow IS
  '공정비교(같은 순간·같은 풀 재매칭) 틱 집계. '
  '⚠2026-08-11 07:12:33Z 경계로 모집단 정의가 바뀌었다(mig 0149): 15분 창 기준이 upd_ts→yt_dis_ts. '
  '⚠2026-08-24 두 번째 경계(dispatched-q-rows): 배차행이 픽업 후가 아니라 배차 ≤60초에 창에 들어온다 '
  '— 창 안 행수·재배열 여지 통계가 경계 전후로 다른 모집단이다. ts 로 갈라 읽을 것.';
