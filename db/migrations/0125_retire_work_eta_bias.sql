-- 0125: 작업도달 **보정을 폐기**한다. 매뷰는 남기되 역할을 바꾼다 — 보정기 → **정확도 계기**.
--
-- ■ 왜 폐기하나 (2026-08-04 사용자 지시 + 실측)
-- 보정이 예측을 다듬는 게 아니라 **삼켰다**. 값이 +1,667~2,547초까지 커지자, 각 크레인의 첫 구역은
-- 앞에 밀린 작업이 0이라 남는 게 보정값뿐이 되어 **크레인이 달라도 전부 같은 값(≈36분 뒤)** 이
-- 나왔다. 그 결과 "가장 급한 일"이 영원히 36분 뒤라 설계 ③(마감으로 후보 풀 구성)이 아무것도
-- 담지 못했다.
--
-- 그리고 **보정 없이가 더 정확했다**. 크레인 26대 대조(2026-08-04, 보정 OFF):
--   예측(가장 이른 상자) 지금−79초  vs  실제 다음 무브 +150초  ⇒ 차이 **201초(3.4분)**
--   "어느 구역을 할지" **80.8%** 적중
-- 보정을 켰을 때는 같은 구간이 28~46분 틀렸다. ⇒ 보정이 없어서 부정확한 게 아니라
-- **보정이 있어서 부정확했다**.
--
-- ■ 무엇이 남고 무엇이 사라지나
-- 사라짐: 예측에 보정을 더하는 동작. 정적 상수 DS_WORK_ETA_BIAS_S(600초)도 함께 제거.
--         (그 상수는 유령전환 버그 시절 손으로 튜닝한 값이었다)
-- 남음:   `learn_work_eta_bias` 매뷰 — 이제 **정확도 계기**다. median(정답 − 예측)을 계속 재므로
--         예측이 나빠지면 여기서 먼저 드러난다. 20분마다 갱신도 그대로 둔다.
--         `dispatch_pred_sample.applied_bias_s` 는 이제 항상 0 이고, 그래서 예측 = 원본예측이다.
--
-- ⚠ 되살리려면: workpool.rs 의 해당 자리에 조회를 되돌리면 된다. 다만 위 실측(201초 vs 28~46분)을
--   뒤집을 근거가 **먼저** 있어야 한다. 그냥 켜면 같은 붕괴가 재현된다.
--
-- ⚠ 판별자 주의: bias_ver=2 구간 중 2026-08-04 이후는 applied_bias_s 가 항상 0 이다. 그 이전
--   구간과 섞어 "보정이 얼마였나"를 집계하면 안 된다.
--
-- 멱등(주석만 갱신).

COMMENT ON MATERIALIZED VIEW learn_work_eta_bias IS
  '★2026-08-04 역할 변경: 보정기 → **정확도 계기**. 이 값은 더 이상 예측에 더해지지 않는다(mig 0125). '
  'median(정답 − 예측)을 재는 용도로만 남긴다 — 예측 정확도가 나빠지면 여기서 먼저 드러난다. '
  '키 = (크레인, 작업유형, 예측거리 10분버킷). horizon_bucket=-1 은 거리 무관.';

COMMENT ON COLUMN dispatch_pred_sample.applied_bias_s IS
  '이 예측에 적용된 보정 초. ★2026-08-04 이후 항상 0 (보정 폐기·mig 0125) ⇒ 예측 = 원본예측. '
  '그 이전 구간과 섞어 집계하지 말 것(bias_ver 로 가르되, 0125 이후는 값 자체가 0 이다).';
